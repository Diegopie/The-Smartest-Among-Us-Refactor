DROP DATABASE IF EXISTS quizApp;
CREATE DATABASE quizApp;

-- USE quizApp;

-- CREATE TABLE Users (
--     id int NOT NULL AUTO_INCREMENT,
--     username VARCHAR(255) NOT NULL UNIQUE,
--     password VARCHAR(255) NOT NULL,
--     PRIMARY KEY (id) 
-- );

-- May update this to create the full db, but may also refactor the hard coded quizzes and to work without the db and write some js to insert seed data